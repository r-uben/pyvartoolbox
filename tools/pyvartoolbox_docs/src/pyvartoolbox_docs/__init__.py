"""Documentation tooling for the pyvartoolbox repository.

Not part of the installed library: these commands shell out to ``pandoc`` and
read repo-relative paths (``skill/graph``, the upstream ``VAR_Handbook.tex``)
that no installed user has. They live here so the wheel carries only the
library.
"""
