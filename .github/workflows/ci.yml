name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync --all-extras
      - run: uv run ruff check .
      # tests/test_packaging.py inspects the built wheel and skips when dist/ is
      # absent. Without this step it skipped on every run, so the guard against
      # dev-only modules re-entering the distribution never actually fired.
      - run: uv build
      - run: uv run pytest --cov=pyvartoolbox --cov-report=term-missing
