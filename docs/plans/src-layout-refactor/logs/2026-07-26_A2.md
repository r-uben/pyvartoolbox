# TICKET-A2 — Drop the doc-tooling scripts from the library distribution

Date: 2026-07-26 · Branch: `feat/10-src-layout-refactor` · Status: DONE (verified, uncommitted at time of writing)

## How this ticket was executed — read this before trusting the status

A2 was **not** dispatched as its own implementer agent. Its substantive edit — removing
`pyvartoolbox-convert-handbook` and `pyvartoolbox-graph-diagram` from the library's
`[project.scripts]` — landed inside A1's diff because the A1 dispatch prompt paraphrased
the ticket and pulled A2's scope in with it. That was a dispatcher error (see
`2026-07-26_A1.md`).

Rather than mark A2 done by assertion, its Done-when gates were run independently
against the built artifact. They pass. The ticket is recorded DONE on that evidence,
with the irregular route documented so the board is not read as "A2 got a normal
implementer+reviewer pass" — it did not. It got an independent verification pass.

## Done-when verification (run by the dispatcher against the built wheel)

- `uv build` → exit 0
- `unzip -l dist/*.whl | grep -cE "_convert|_graph"` → `0`
- `unzip -p dist/*.whl '*/entry_points.txt' | grep -cE "convert-handbook|graph-diagram"` → `0`
- full `entry_points.txt` contents of the wheel:
  ```
  [console_scripts]
  pyvartoolbox-replicate = pyvartoolbox.replications:main
  ```
- `uv run pyvartoolbox-graph-diagram --help` → exit 0, resolving to
  **`/Users/rubenffuertes/repos/open-source/pyvartoolbox/.venv/bin/pyvartoolbox-graph-diagram`**
  — the project venv, not a stale global install (the ticket's specific concern)
- `uv run pytest -q` → 252 passed

The wheel check is the one that matters: asserting against the source tree would miss a
hatch `artifacts`/`packages` setting silently re-including files. The A1 reviewer built
and inspected the wheel independently and reached the same result.

## Note

`dist/` is gitignored (`.gitignore:8`), so the build artifacts produced during
verification do not pollute the working tree.
