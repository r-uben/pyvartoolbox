# TICKET-A1 — Relocate doc tooling out of the library package

Date: 2026-07-26 · Branch: `feat/10-src-layout-refactor` · Status: DONE (reviewed, uncommitted at time of writing)

## What shipped

`_convert.py` and `_graph.py` moved out of `src/pyvartoolbox/` into a new repo-level
`tools/pyvartoolbox_docs/` package with its own `pyproject.toml` (hatchling, matching
the root's `requires-python = ">=3.11"`, `dependencies = []` — pandoc is shelled out
via subprocess, so there is no pypandoc import to declare). Wired as a uv workspace
member with `pyvartoolbox-docs` in `[dependency-groups] dev` only. Imports in
`tests/test_concept_graph.py` and `tests/test_handbook.py` updated, import-path-only.

Files: `pyproject.toml`, `tools/pyvartoolbox_docs/**` (new),
`tests/test_concept_graph.py`, `tests/test_handbook.py`, removal of
`src/pyvartoolbox/_convert.py` and `src/pyvartoolbox/_graph.py`.

## Done-when verification (run by the dispatcher, not the implementer)

- `ls src/pyvartoolbox/_convert.py src/pyvartoolbox/_graph.py` → exit 1
- `uv run pytest -q` → 252 passed (see the count correction below)
- tests/ diff import-only grep → `0`
- additionally: `ruff check` clean; all three console scripts exit 0 under `uv run`;
  `pyvartoolbox.__all__` byte-identical to the `a5710ec` baseline

## Test-count correction

A1's Done-when originally read "249 tests collected". Verified against `a5710ec` by
stashing: the baseline is **252**, with per-file collection counts identical to the
post-refactor tree. The 249 was stale when the plan was written — no test was added,
lost, or renamed. `TICKETS.md` and `STATUS.md` corrected on 2026-07-26.

## Dispatch error to note

The A1 implementer prompt instructed moving the two console-script entries out of the
root `[project.scripts]`. **That is TICKET-A2's change, not A1's** — an error in the
dispatch prompt, not by the implementer, which executed its instructions faithfully.
The consequence is that A2's substantive edit landed inside A1's diff. A2 is recorded
separately (`2026-07-26_A2.md`) with its own independently-run gates rather than being
silently absorbed. Lesson for later waves: build the implementer prompt from the ticket
text verbatim; paraphrasing leaked the neighbouring ticket's scope in.

## Review

`code-reviewer` pass: APPROVE, no blocking findings. Independently verified beyond the
dispatcher's checks: relocated files are byte-identical to their pre-move versions;
their path handling is cwd-relative (not `__file__`-relative) so the move is safe;
`pyvartoolbox-docs` sits in `[dependency-groups] dev` (PEP 735), separate from project
metadata, so it cannot leak into the published wheel's runtime deps; repo-wide grep
finds no stale `pyvartoolbox._convert` / `._graph` references in code, CI, or docs;
`.github/workflows/ci.yml` needs no change (`uv sync --all-extras` installs default
dependency-groups; `--all-extras` governs optional-dependencies, not groups).

## Carried forward

- `uv.lock` is gitignored in this repo (pre-existing convention) and has not been
  re-locked for the new workspace member. The next `uv sync` will re-lock; this is
  expected, not drift.
- `skill/**` still references the console-script *names*, which remain valid. Rewording
  them as maintainer-only tooling is TICKET-A3, correctly left untouched here.
